可以正确创建账户，激活码与名称不可用时需要重试。检查点：创建成功的截图
可以正确导入账户，可以是固定助记词。检查点：导入成功的截图，要求名称一致

准备OOD环境，查找并激活。检查点：激活成功截图
补充关键测试目标：
- Active DID 切换后，首页、SN 状态、OOD 状态、BuckyApi 返回身份信息需要保持一致
- 密码错误、取消操作、重复导入、无 SN 记录导入失败等异常路径需要覆盖
- BuckyApi 的关键接口，特别是 signJsonWithActiveDid 的 signatures、pwd_hash、错误码与 busy/cancel 行为需要覆盖
- 删除身份后，本地缓存与持久化状态不能串到其他 DID

到不了检查点需要说明卡在哪一步

代码审查：审查当前代码的安全性，确保没有高风险代码
日志扫描：有没有敏感日志
持久化数据审查：有没有保存敏感信息
以上三项的高风险及敏感信息是指：私钥，助记词，密码及其泄露风险。

生成测试报告
